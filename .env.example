# Copy to .env (git-ignored) for local dev, and set the same in Vercel
# (Project -> Settings -> Environment Variables) for Production AND Preview.
# Server-only secret: used by src/pages/api/ideas.ts. NEVER prefix with PUBLIC_.
NVIDIA_API_KEY=
